# Environment Configuration
# Set to 'production' in Vercel environment variables
NODE_ENV=development

# Email Configuration - Recipient Addresses
# Configure these in Vercel environment variables for production
RATE_REQUEST_EMAIL=operations@seashipping.com
CONTACT_EMAIL=info@seashipping.com
NEWSLETTER_EMAIL=marketing@seashipping.com

# Email Service - Resend API Key
# Sign up at https://resend.com to get your API key
# IMPORTANT: Set this in Vercel environment variables, not in code
RESEND_API_KEY=your_resend_api_key_here

# Google Analytics 4 - Tracking ID
# Only active in production environment
# This is public-facing and safe to commit
NEXT_PUBLIC_GA_ID=G-V0F46NZK7J

# reCAPTCHA (if needed for form protection)
# Get keys from https://www.google.com/recaptcha/admin
# RECAPTCHA_SECRET_KEY=your_secret_key_here
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_site_key_here

# Deployment Notes:
# - .env.local should NEVER be committed to git
# - Set all sensitive values in Vercel Dashboard → Project Settings → Environment Variables
# - Use different values for Production, Preview, and Development environments
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Regular environment variables are only available server-side
